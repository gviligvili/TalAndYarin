import { ESPMarkerDirective } from './espmarker.directive';

describe('ESPMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new ESPMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
